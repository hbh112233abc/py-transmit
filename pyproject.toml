[project]
name = "transmit"
version = "0.4.2"
description = "Transmit Server & Client use thrift"
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
    "loguru>=0.7.3",
    "pydantic>=2.5.3",
    "thrift>=0.22.0",
]
